{
  "scripts": {
    "start": "node ./api/bin/www",
    "build-client": "cd client && npm run build",
    "install-client-reqs": "cd client && npm install",
    "install-api-reqs": "cd api && npm install",
    "heroku-postbuild": "npm run install-api-reqs"
  }
}